import 'package:flutter/material.dart';

class ServiceIntro extends StatelessWidget {
  const ServiceIntro({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("서비스 인트로 페이지"),
    );
  }
}
